#include<stdio.h>
 
  int main()
  {
    int  avg , a, b, f ;
     printf("entre the value of a,b,f");
       scanf("%d%d%d",&a,&b,&f);
          avg= a+b+f;
            printf("avg=%d",avg);
               return 0;

  }